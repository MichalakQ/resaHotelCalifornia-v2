const ClientController = require('../controllers/clientController');
const { Client } = require('../models');

const mockClient = {
    findAll: jest.fn(),
  };

describe('doit afficher la liste des clients',() => {



})